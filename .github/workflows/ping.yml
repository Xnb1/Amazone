name: Ping Render App

on:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Render
        run: curl https:///amazone-54bq.onrender.com
